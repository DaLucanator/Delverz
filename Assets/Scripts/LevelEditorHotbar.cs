using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelEditorHotbar : MonoBehaviour
{
    //this script manages input for control of a hotbar that store tile for placement in the level editor
    //the player switches their current tile using input via arrow keys or arcade machine buttons
    //the hotbar visually updates to display which tile is currently selected

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
